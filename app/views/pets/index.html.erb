<% @pets.each do |pet| %>
 <p>Name: <%= link_to pet.name, swipes_path %></p>
 <p>Breed:<%= pet.breed %></p>
 <p>Gender:<%= pet.gender %></p>
 <p>Age:<%= pet.age %></p>
 <p> <%= link_to "Delete this pet", pet_path(pet), method: :delete %>
 <% end %>

 <%= link_to "Add Pet", new_pet_path %>
